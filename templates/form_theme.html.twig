{% block corvet_masked_date_widget %}
	<fieldset class="corvet--light-date-widget" data-controller="corvet--light-field-bundle--light-date-field">
		<legend>{{ legend }}</legend>

		{{ block('form_widget_simple') }}

		<button type="button" class="masked-date-btn">
			📅
		</button>
	</fieldset>
{% endblock %}
