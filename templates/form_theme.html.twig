{% block corvet_masked_date_widget %}
	<fieldset class="masked-date-widget" data-controller="corvet--light-field-bundle--main">
		<legend>{{ legend }}</legend>

		{{ block('form_widget_simple') }}

		<button type="button" class="masked-date-btn">
			📅
		</button>
	</fieldset>
{% endblock %}
